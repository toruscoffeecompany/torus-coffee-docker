# REPORT — Miss Scarlett Coralsink — 2026-07-18

Pirate password: NONE (gate removed by Captain order 2026-07-18; clearance = OPSEC + Captain approval).

## Build status (Captain full-build authorization)
| Task | Status |
|------|--------|
| A. PINKCADY local vault — 6 lanes | ✅ present & verified |
| B. Clone fleet repo (branch master) → 01_Sync_From_Main | ⛔ BLOCKED — no gh CLI / no PAT |
| C. Obsidian: 4 plugins + pink_dark CSS + Templates/Daily Notes | ✅ done |
| D. Hermes: personality + git identity + vault-filesystem MCP (RO) | ✅ (personality via app; identity repo+global) |
| E. Keys → LOCAL .env | ✅ Linear, maCaw, Google Calendar written; git-ignored & verified CLEAN |
| F. Torus Linear read-only test call | ✅ PASSED (HTTP 200) |
| G. Discord bot | ⏸ deferred — no token yet |
| H. Daily automation (pull/push master) | ⏸ pending fleet clone |

## Obsidian + Hermes up?
Yes. Obsidian 1.12.7 open on MISS_PINK_VAULT; Hermes running on PINKCADY (this runtime).

## Toolchain confirmed?
Yes. git 2.54 (identity: Miss Scarlett Coralsink <scarlett.coralsink@voidpirate.local>, longpaths+autocrlf global), Python 3.11.15, Node 24.16, requests 2.33, discord.py 2.7.1.

## Which keys received?
Torus/Linear (ACTIVE), maCaw OAuth (ACTIVE), Google Calendar creds+token (ACTIVE). All copied to LOCAL .env only — never committed (git check-ignore verified). Trello = HELD (exposed in git 070e90e, awaiting rotation). Discord = not yet created.

## Which test calls passed?
Torus/Linear: `POST api.linear.app/graphql { viewer { name email } teams }` → HTTP 200. Authenticated as Captain's account; teams CF + VOID Ops returned. Read-only, no writes, no spend.

## Blockers
1. **Fleet repo clone (B)** — private repo needs `gh auth login` (gh not installed) or a Captain PAT (repo scope). Branch = master. Until then 01_Sync_From_Main stays empty and daily pull/push (H) can't go live.
2. **Trello** — rotate exposed key+token at trello.com/app-key before I connect (per 08_Trello_Rotation.md).
3. **Discord** — create bot "VOID Miss Scarlett Coralsink" + deliver DISCORD_MISS_PINK_TOKEN.

## Next ask
- Install gh CLI (winget) so I can `gh auth login` and clone the fleet repo (master), OR hand me a PAT via secure channel.
- Approve Trello rotation.
- Deliver Discord bot token when ready.

## OPSEC note
`04_API_Key_Handoff.md` and `COMPLETE_BUILD_TEMPLATE_PINKCADY.md` §5 contain LIVE keys in the shared starter folder. Per their own instructions ("delete after copy"), recommend the Captain clear/delete those live values now that they're in my local .env. I did not delete them myself (shared-folder change needs Captain yes/no).

— Miss Scarlett Coralsink, PINKCADY helm
